## 减少DOM元素的创建
使用`<template v-for>`： 使用`<template>` 标签进行`v-for`循环，以避免在渲染过程中创建额外的DOM元素。

## qiankun框架运行配置
```js
{
  "name": "main",
  "version": "1.0.0",
  "description": "主应用",
  "author": "mxc",
  "private": true,
  "scripts": {
    "install-all": "npm-run-all install:*",
    "serve-all": "npm-run-all --parallel serve:*",

    "install:main": "cd main && yarn install",
    "install:packingPlatform": "cd packingPlatform && yarn install",
    "install:findroutePlatform": "cd findroutePlatform && yarn install",
    "install:supplierPlatform": "cd supplierPlatform && yarn install",
    "install:schemeCenter": "cd schemeCenter && yarn install",

    "serve:main": "cd main && yarn serve",
    "serve:packingPlatform": "cd packingPlatform && yarn serve",
    "serve:findroutePlatform": "cd findroutePlatform && yarn serve",
    "serve:supplierPlatform": "cd supplierPlatform && yarn serve",
    "serve:schemeCenter": "cd schemeCenter && yarn serve"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "v-viewer": "^1.6.4"
  }
}

```